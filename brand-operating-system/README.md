## Regeneración de Contenido

El único comando oficial y seguro para regenerar o expandir el calendario editorial es:

```bash
node generateDeterministic.cjs
```

**ADVERTENCIA:** No utilices generadores antiguos (ahora en `_archive/`). `generateDeterministic.cjs` es determinista y preserva ediciones manuales.
